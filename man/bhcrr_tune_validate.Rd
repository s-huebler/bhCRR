% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning_diagnostics.r
\name{bhcrr_tune_validate}
\alias{bhcrr_tune_validate}
\title{Validate a tuning data frame from tune_ssl_psdh()}
\usage{
bhcrr_tune_validate(
  tuning,
  n,
  theta,
  s0_seq,
  s1_seq,
  beta_min = 0.1,
  gap_band = c(0.5, 2),
  s1_fixed = length(unique(s1_seq)) < 2L
)
}
\arguments{
\item{tuning}{Data frame returned by \code{\link{tune_ssl_psdh}}.}

\item{n}{Integer. Number of observations.}

\item{theta}{Numeric in (0, 1). Estimated non-zero proportion.}

\item{s0_seq, s1_seq}{Numeric vectors defining the grid that was searched.}

\item{beta_min}{Numeric. Clinically relevant minimum treatment effect.}

\item{gap_band}{Length-2 numeric. Multiplicative band around \code{beta_min}
the optimum's zero gap should fall in. Default \code{c(0.5, 2)}.}

\item{s1_fixed}{Logical. Was \code{s1} coerced to a single value rather than
searched? Defaults to \code{TRUE} when \code{s1_seq} holds one distinct
value. A fixed \code{s1} is not a searched dimension, so the optimum can
never be "on its boundary"; the edge test is skipped for it and only
\code{s0} can trigger a widen.}
}
\value{
A list: \code{checks}, \code{all_pass}, \code{best} (one-row data
frame), \code{on_boundary}, \code{boundary_dims}, \code{gap_at_opt}.
}
\description{
Checks that (1) exactly one grid point attains the max score, (2) that
maximiser is interior to the grid (not on an edge - which would mean the
optimum likely lies outside the searched region), and (3) the zero gap at
the optimum is within a sane band around the clinical target.
}
\seealso{
\code{\link{bhcrr_autotune}}
}
