% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{cpp_cv_cindex}
\alias{cpp_cv_cindex}
\title{Cross-validation C-index across a lambda path (RcppArmadillo backend)}
\usage{
cpp_cv_cindex(
  x_test_,
  coef_,
  time_test_,
  status_test_,
  tuning,
  evaluation_time,
  reverse = FALSE
)
}
\arguments{
\item{tuning}{Either \code{"normal"} (Harrell C-index, matching
\code{survival::concordance}) or \code{"wolbers"} (IPCW competing-risks
C-index, matching \code{wolbers_c}).}

\item{evaluation_time}{Numeric scalar; the time horizon used only by the
\code{"wolbers"} tuning. Ignored when \code{tuning = "normal"}.}

\item{reverse}{Logical; orientation of the Harrell C-index for
\code{tuning = "normal"}. \code{FALSE} (default) matches the
\code{survival::concordance} default. Ignored when
\code{tuning = "wolbers"}.}

\item{x_test}{Numeric matrix (n_test x p) of held-out predictors.}

\item{coef}{Numeric matrix (p x n_lambda) of fitted coefficients along the
lambda path.}

\item{time_test}{Numeric vector of held-out event/censoring times.}

\item{status_test}{Numeric vector of held-out status codes (0 = censored,
1 = cause-1 event, 2 = competing event).}
}
\value{
Numeric vector of length \code{ncol(coef)} of C-index values
(\code{NA} where no comparable pairs exist).
}
\description{
Computes, for each column of \code{coef} (one lambda), the test-fold
concordance of the risk scores \code{x_test \%*\% coef[, j]}. This is the
compiled kernel behind \code{cv_fastCrrp_cpp()}.
}
\keyword{internal}
