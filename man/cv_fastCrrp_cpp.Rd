% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_fastCrrp_cpp.r
\name{cv_fastCrrp_cpp}
\alias{cv_fastCrrp_cpp}
\title{Cross-validated Tuning for fastCrrp (RcppArmadillo backend)}
\usage{
cv_fastCrrp_cpp(
  x,
  time,
  status,
  k = 5,
  penalty = "LASSO",
  lambda_path = NULL,
  tuning = "normal",
  eval_quantile = 0.5,
  reverse = FALSE
)
}
\arguments{
\item{x}{Numeric predictor matrix (n x p).}

\item{time}{Numeric vector of event/censoring times.}

\item{status}{Numeric vector of status codes (0 = censored, 1 = cause-1
event, 2 = competing event).}

\item{k}{Integer number of cross-validation folds.}

\item{penalty}{Penalty passed to \code{\link[fastcmprsk:fastCrrp]{fastcmprsk::fastCrrp()}}.}

\item{lambda_path}{Optional numeric vector of lambda values; if \code{NULL}
the path is chosen by \code{\link[fastcmprsk:fastCrrp]{fastcmprsk::fastCrrp()}}.}

\item{tuning}{Either \code{"normal"} (Harrell C-index, matching
\code{survival::concordance}) or \code{"wolbers"} (IPCW competing-risks
C-index, matching \code{\link[=wolbers_c]{wolbers_c()}}).}

\item{eval_quantile}{Quantile of the training times used as the evaluation
horizon for \code{tuning = "wolbers"}.}

\item{reverse}{Logical orientation flag for the \code{"normal"} Harrell
C-index. \code{FALSE} (default) matches the \code{survival::concordance}
default. Ignored when \code{tuning = "wolbers"}.}
}
\value{
A list with elements \code{lambda}, \code{cv_c_index},
\code{lambda_min}, \code{full_model}, and \code{cv_raw_matrix}, matching
\code{cv_fastCrrp()}.
}
\description{
Cross-validates a \pkg{fastcmprsk} fine-Gray LASSO path, delegating the
per-lambda prediction and the O(n^2) concordance computations to compiled
RcppArmadillo kernels (see \code{src/cv_fastcrrp.cpp}).  Model fitting is
performed by \code{\link[fastcmprsk:fastCrrp]{fastcmprsk::fastCrrp()}}.  Used internally by the
\code{"LASSO_cv"} initialization method in \code{\link{bhcrr_cv_control}}.
}
\examples{
\dontrun{
cv_fastCrrp_cpp(x, time, status, k = 5, penalty = "LASSO",
                tuning = "wolbers", eval_quantile = 0.5)
}
}
\seealso{
\code{\link[=wolbers_c]{wolbers_c()}}
}
